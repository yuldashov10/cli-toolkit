# Copy to .env and fill in your values
# .env overrides config.yaml, but NOT already-set environment variables

APP_NAME=cli-toolkit

# DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO

DOWNLOADS_DIR=~/Downloads

# --- Add project-specific secrets below (never commit .env) ---
# API_KEY=
# DB_URL=
